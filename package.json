{
  "private": true,
  "name": "medic",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format-root": "yarn prettier --write *.json *.md",
    "format": "yarn format-root && yarn workspace @minodisk/medic format && yarn workspace @minodisk/medkit format && yarn workspace @minodisk/medmd format",
    "build": "yarn format-root && yarn workspace @minodisk/medic build && yarn workspace @minodisk/medkit build && yarn workspace @minodisk/medmd build",
    "test": "yarn workspace @minodisk/medic test && yarn workspace @minodisk/medkit test && yarn workspace @minodisk/medmd test"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
